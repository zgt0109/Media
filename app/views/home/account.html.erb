<% content_for :main_content do %>
  <div class="main-content">
    <div class="breadcrumbs" id="breadcrumbs">
      <ul class="breadcrumb">
        <%= render 'partials/home' %>
        <li><%= link_to '系统设置', account_url %></li>
        <li><%= link_to '我的账号', 'javascript:;' %></li>
      </ul>
      <%= render '/layouts/qrcode' %>
    </div>

    <div class="page-content">
      <div class="row">
        <div class="col-sm-12">
          <table id="" class="table table-striped table-bordered table-hover dataTable ">
            <tbody>
            <tr>
              <th colspan="2">我的账号</th>
            </tr>
            <tr>
              <td>登录账号：</td>
              <td><%= current_user.nickname %></td>
            </tr>
            <tr>
              <td>用户ID：</td>
              <td><%= current_user.id %></td>
            </tr>
            <tr>
              <td>手机号码：</td>
              <td><%= current_user.tel %></td>
            </tr>
            <tr>
              <td>电子邮件：</td>
              <td><%= current_user.email %></td>
            </tr>
            </tbody>
          </table>

          <%# if @wx_mp_user %>
            <%# @allow_rebind = @wx_mp_user && (@wx_mp_user.binds_count <= 1) && !@wx_mp_user.froze? %>
            <!--table id="" class="table table-striped table-bordered table-hover dataTable margin-top-30">
              <tr>
                <th colspan="5" class="">公众账号信息</th>
              </tr>
              <tr>
                <td>公共账号</td>
                <td>Token</td>
                <td>URL</td>
                <td width="10%">状态</td>
                <td width="180">操作</td>
              </tr>
              <tr>
                <td><%= @wx_mp_user.name %></td>
                <td><%= @wx_mp_user.token %></td>
                <td><%= @wx_mp_user.url %></td>
                <td><span class="<%#= @wx_mp_user.active? ? 'green' : 'red' %>"><%= @wx_mp_user.status_name %></span></td>
                <td>
                  <!--<a href="javascript:;" onclick="pop(this)" data-name="name" data-w="330" data-h="200" data-title="重新绑定" data-bd="<div class='box-form form-row'><form><p><%#= @allow_rebind ? '您只有一次重新绑定的机会，确定重新绑定吗？' : '您已经绑定过两次公众账号，无法重新绑定!' %></p><p class='tcenter'><a href='<%#= @allow_rebind ? '/binds/rebind' : 'javascript:;' %>' class='btn btn-small btn-close btn-orange'>确认</a>　　 　<%# if @allow_rebind %><a class='btn btn-small btn-close'>取消</a><%# end %></p></form></div>">重新绑定</a>-->
                  <!--a href="<%#= rebind_binds_url %>">重新绑定</a>
                  <%# if current_user.wx_mp_user %><a class="margin-left-10" href="javascript:;" data-toggle="modals" data-height="350" data-iframe="<%= qrcode_wx_mp_users_url %>" data-title="二维码管理">二维码管理</a><%# end %>
                </td>
              </tr>
            </table>
          <%# else %>
            <p class="p-but-til">
              <input type="button" value="" class="bundling" onclick="javascript:location.href='<%= rebind_binds_url %>'"/>
            </p-->
          <%# end %>

        </div>
      </div>
    </div>
  </div>
<% end %>
