<header>
  <a href="<%= mobile_trips_url(site_id: @site.id) %>" class="ico-prev tleft"><img src="/assets/mobile/wtrave/ico-back.png"></a>
  <h1>门票详情</h1>
  <a href="<%= mobile_trip_orders_url %>" class="tright"><img src="/assets/mobile/wtrave/ico-order.png"></a>
</header>

<section>
  <div class="box box-order">
    <dl>
      <dt>门票信息</dt>
      <dd>
        <div class="order-row">
          <div class="fl">分类：</div>
          <div class="fr"><span><%= @trip_ticket.trip_ticket_category %></span></div>
        </div>
        <div class="order-row">
          <div class="fl">名称：</div>
          <div class="fr"><span><%= @trip_ticket.name %></span></div>
        </div>
        <div class="order-row">
          <div class="fl">单价：</div>
          <div class="fr fred"><span>￥<%= f @trip_ticket.price %></span></div>
        </div>
      </dd>
    </dl>
    <dl>
      <dt>门票描述</dt>
      <dd>
        <div class="order-row">
          <% if @trip_ticket.pic_url.present? %><p class="fl"><img src="<%= @trip_ticket.pic_url %>"></p><% end %>
          <p class="fl"><%= @trip_ticket.description %></p>
        </div>
      </dd>
    </dl>
    <p class="tcenter">
      <a class="btn fr" href="<%= new_mobile_trip_order_path(ticket_id: @trip_ticket.id) %>">在线预定</a>
    </p>
  </div><!--box-order end -->

  <div class="box-note">
    <b>预定须知</b>
    <p><%= @trip_ticket.content.to_s.html_safe %></p>
  </div>
</section>