$('.box-comment').append('<%= j render(:partial => "comment") %>');
<% if @comments.try(:last_page?) %>
  $('.page').remove();
<% else %>
  $('.page').replaceWith('<%= j paginate(@comments) %>');
<% end %>
$(".b-loading").hide();