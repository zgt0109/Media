<%= form_for(@api_wmall_product) do |f| %>
  <% if @api_wmall_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_wmall_product.errors.count, "error") %> prohibited this api_wmall_product from being saved:</h2>

      <ul>
      <% @api_wmall_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
