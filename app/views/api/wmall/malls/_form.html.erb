<%= form_for(@api_wmall_mall) do |f| %>
  <% if @api_wmall_mall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_wmall_mall.errors.count, "error") %> prohibited this api_wmall_mall from being saved:</h2>

      <ul>
      <% @api_wmall_mall.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
