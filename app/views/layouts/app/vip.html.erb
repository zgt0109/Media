<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="user-scalable=no, initial-scale=1.0, maximum-scale=1.0, width=device-width">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta content="black" name="apple-mobile-web-app-status-bar-style">
  <meta name="format-detection" content="telephone=no">
  <meta content="telephone=no" name="format-detection">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <title><%=  content_for?(:title) ? yield(:title) : "会员卡" %></title>
  <script>
    document.addEventListener('WeixinJSBridgeReady', function onBridgeReady() {
      WeixinJSBridge.call('hideOptionMenu');
    });
    aaa = 3
    winwemedia = {
      validators: {
        mobile: /^1[34578]\d{9}$/
      }
    };
  </script>
  <%= stylesheet_link_tag "mobile/jquery-ui-1.10.3.custom.min" if action_name == "signup" %>

  <% if action_name =~ /index|description|shops|privileges|info|notes|gifts|exchanged|consumes|points|use_gift|print|signin|recharge_back|go_recharge|get_gift|vip_packages|vip_package_show|transaction|my_consume_show|buy_vip_package|by_usable_amount|buy_success|activate|signup/ %>
    <%= stylesheet_link_tag "mobile/wcard" %>
  <% else %>
    <%= stylesheet_link_tag "mobile/wmember" %>
  <% end %>

  <%= render 'app/vips/partials/vip_card_data' %>
  <%= javascript_include_tag 'mobile/vip_card' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <%= render 'application/gb2big5' %>
  <%= yield %>

  <%= render "application/app_footer" %>
</body>
<script>
    <% if flash[:notice] || flash[:alert] %>
    alert("<%= flash[:notice] || flash[:alert] %>");
    <% end %>
</script>
</html>
