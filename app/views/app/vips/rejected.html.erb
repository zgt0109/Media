<div class="html theme-<%= @vip_card.template_id %> <%= "no-card" if !@vip_user.try :normal? %> <%= "add-nav" if @vip_card.template_id == 2 %>" id="html">
  <div class="stage index" id="stage">
    <!-- 会员卡首页 start -->
    <section id="sec-index">
        <%= render "app/vips/partials/vip_card_cover" %>
        <div class="card_check">
            <span><%= image_tag "mobile/wcard/reject.png" %></span>
            <h1>亲，因为无缘!</h1>
            <h1>我们不能受理您的会员申请请求。</h1>
        </div>
    </section>
    <!-- 会员卡首页 end -->
  </div>
  <%= render 'footbar' %>
</div>
